/*
  Stack: LIFO / Queue: FIFO
  1. arr(stack).push(el) = add one or more el to end of assigned stack
  2. arr(stack).pop() = removes and returns the last el of assigned stack
  3. arr(stack).shift() = removes and return the first el of assigned stack
  4. arr(stack).unshift() = add one or more el to beginning of assigned stack

*/
